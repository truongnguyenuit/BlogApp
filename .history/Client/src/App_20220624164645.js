import {
  BrowserRouter,
  Routes,
  Route,
} from "react-router-dom";
import 

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={} />
        <Route path="/" element={} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
